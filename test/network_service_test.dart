import 'package:test/test.dart';

void main() {
  // NetworkService networkService;
  setUpAll(() {
    // networkService = NetworkService();
  });

  group('NetworkService', () {
    test('connect', () {});
  });
}
